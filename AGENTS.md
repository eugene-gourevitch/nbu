## Learned User Preferences
- Prefers sensitive legal/compliance content to use neutral, analytical framing instead of directive or prosecutorial wording.
- Wants contested claims to include explicit attribution and non-verification caveats while preserving underlying facts and citations.
- When executing an attached plan with existing todos, expects the existing todo list to be progressed in order rather than recreated.
- Prefers fast shareable previews (Vercel URLs) and explicit confirmation that finished drafts are on `main`.

## Learned Workspace Facts
- The primary working repository is `C:/Users/egour/nbu` and active delivery branch expectations center on `main`.
- Slide deck content is maintained in `src/components/slides/slideData.tsx`.
- The workspace can produce Vercel preview deployments for this repo.
